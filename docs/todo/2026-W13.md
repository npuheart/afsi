# TODO — Saturday 21 March 2026

## 1. Incompressible Navier-Stokes solver classes


- [x] Implement **Chorin projection (fractional-step)** solver class — `src/afsic/euler/ChorinSolver.py`.
- [x] Implement **IPCS (Incremental Pressure Correction Scheme)** solver class — `src/afsic/euler/IPCSSolver.py`.
- [x] Add unit tests (`tests/test_ns.py`)，which is the lid-driven cavity problem.

- [] Design a base class / interface for incompressible NS solvers — **`NSBase`** in `src/afsic/euler/NSBase.py`.


## 2. Coupling function and tests

- [ ] Implement the IB coupling function that maps between Eulerian (fluid) and
  Lagrangian (solid) grids using the existing `IBMesh` / `IBMesh3D` infrastructure.
- [ ] Add unit tests:
  - [ ] 2-D coupling: verify force spreading and velocity interpolation on a
    known analytic field.
  - [ ] 3-D coupling: same checks on a simple 3-D domain.
  - [ ] Mass/force conservation across the interface.

## 3. Duality tests for interpolation and spreading operators

- [ ] Verify the **adjoint (duality) relation**:
  $\langle J f, v \rangle_{\Omega_f} = \langle f, J^* v \rangle_{\Omega_s}$
  where $J$ is the spreading operator and $J^*$ its adjoint (interpolation).
- [ ] Implement as a pytest parametrized test over 2-D and 3-D, multiple mesh
  resolutions, and multiple quadrature orders.


# TODO — Sunday 22 March 2026

## 1. FSI benchmark test with exact result

- [ ] Identify a fully-coupled FSI problem that admits an analytical or
  well-established reference solution, e.g.:
  - Turek–Hron FSI benchmark (2-D flexible bar behind a cylinder).
  - Oscillating elastic beam in viscous flow (semi-analytic solution).
- [ ] Implement the FSI driver (fluid solver + solid solver + coupling loop).
- [ ] Add a pytest test that:
  - [ ] Runs the benchmark to steady state or one full period.
  - [ ] Compares tip displacement / drag / lift against the reference values
    within a prescribed tolerance.
  - [ ] Checks convergence order under mesh refinement.

