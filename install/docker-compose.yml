services:
  mapengfei_afsi_dev:
    image: mapengfei/dolfinx:local
    container_name: afsi_dev
    command: tail -f /dev/null
    volumes:
      - ../..:/root
    shm_size: '8gb'   # 增加共享内存


# sudo docker compose up -d
# sudo docker exec -it mapengfei_dolfinx_local /usr/bin/bash
# sudo docker run --rm -v $PWD:/home/shared -w /home/shared -it ghcr.io/finsberg/fenicsx-ldrb bash